<% content_for :title, "New Step - ASAP" %>
<div class="max-w-4xl mx-auto py-10 px-4 sm:px-6 lg:px-8">
  <div class="mb-8">
    <h1 class="text-3xl font-semibold text-gray-900 dark:text-white">New Step</h1>
    <p class="mt-2 text-gray-600 dark:text-gray-300">
      Create a new analysis step.
    </p>
  </div>

  <%= render 'form', step: @step %>
</div>

